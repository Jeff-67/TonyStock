# Instructions

During interaction with the user, if you find anything reusable in this project (e.g. version of a library, model name), especially about a fix to a mistake you made or a correction you received, you should take note in the `Lessons` section in the `.cursorrules` file so you will not make the same mistake again.

You should also use the `.cursorrules` file as a scratchpad to organize your thoughts. When receiving a new task:
1. Review the scratchpad content
2. Clear old different task if necessary
3. Explain the new task
4. Plan the steps needed to complete the task
5. Use todo markers to track progress: [X] Done, [ ] Pending
6. Update progress when subtasks are completed

# Tools Configuration

1. Environment Setup
   - Activate conda environment 'stock' before running tools
   - Use run_terminal_cmd for executing Python scripts
   - Set appropriate timeout values for web scraping

2. Data Collection & Analysis Tools
   a. search_engine.py
      - Command: conda activate stock && python -m tools.search_engine "search query" [--max-results N]
      - Quote multi-word queries
      - Default max results: 10
      - Uses DuckDuckGo with API/HTML fallback
      - Debug logging included
      - Returns URLs that should be passed to web_scraper.py for full content

   b. web_scraper.py
      - Command: conda activate stock && python -m tools.web_scraper [URLs...] [--max-concurrent N] [--debug]
      - Used after search_engine.py to get full content from URLs
      - URLs must be complete (with http:// or https://)
      - Quote URLs with special characters
      - Default max concurrent: 5
      - Timeout: 30s with auto-retry
      - Debug flag available

   c. read_pdf.py
      - Command: conda activate stock && python -m tools.read_pdf [--pdf_path pdf_path] [--model model_name]
      - Default model: gpt-4o
      - Outputs markdown in same directory

   d. llm_api.py
      - Supports OpenAI, Anthropic, and local models
      - Default models:
        * OpenAI: gpt-4o
        * Anthropic: claude-3-sonnet-20240229
        * Local: Qwen/Qwen2.5-32B-Instruct-AWQ



# Lessons

1. httpx AsyncClient Connection Issues
   - Problem: httpx.ReadTimeout errors when using AsyncClient across multiple event loops
   - Solution: Created utils/asyncio_support.py with async_http_connections_expire_immediately context manager
   - Implementation: Patched httpcore.AsyncHTTPConnection.__init__ to set keepalive_expiry=0
   - Usage: Wrap all async API calls with this context manager
   - Applied to: agents/trial_agent.py and tools/llm_api.py
   - Reference: https://github.com/encode/httpx/discussions/2959

2. Type Handling in Async Functions with Exception Handling
   - Problem: Type error when appending results from asyncio.gather() with return_exceptions=True
   - Solution: Always convert results to the expected type before appending to a typed list
   - Implementation: Use isinstance() to check for exceptions and handle them separately
   - Example: `processed_results.append(str(result) if not isinstance(result, str) else result)`
   - Applied to: tools/web_scraper_by_ai.py

# Scratchpad

Current Task: Fix missing docstring in utils/asyncio_support.py
- The issue is a D100 error (missing docstring in public module)
- Need to add a module-level docstring that explains the purpose of the module

Plan:
[X] Read the file to understand its purpose and functionality
[X] Add a comprehensive module-level docstring
[X] Test the changes by running flake8
[X] Document the fix in the .cursorrules file
